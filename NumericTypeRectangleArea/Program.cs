using System;

namespace NumericTypeRectangleArea
{
    class Program
    {
        static void Main(string[] args)
        {
            double w;
            double l;
            double rr;

            Console.WriteLine("To get the area of a rectangle we need the width and length." +
                "\n What is the width?");
            w = Console.ReadLine();
            Console.WriteLIne("What is the lenght?");
            l = Console.ReadLine();

            Console.ReadLine();
        }
    }
}
